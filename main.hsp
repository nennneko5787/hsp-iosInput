#include "hsp3dish.as"

	objsize 300, 60
    pos 10, 10
	button gosub "キーボード表示", *openKeyBoard

	repeat
    redraw 1
    await (1000 / 60)
	loop

*openKeyBoard
    dialog "キーボードオープン！"
    devcontrol "open_keyboard"
    return
